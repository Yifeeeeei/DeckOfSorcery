# DeckOfSorcery
Contains script for building the cards from sheets

# 首次使用

安装

```bash
pip install -r requirements.txt
```

生成配置文件

```bash
python generate_default_jsons.py
```

修改相关mass_producer_params_xlsx.json配置

# 生成卡牌

```bash
python run_mass_producer.py
```

将output文件夹移动到需要的位置